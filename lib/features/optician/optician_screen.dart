import 'package:flutter/material.dart';

class OpticianScreen extends StatelessWidget {
  const OpticianScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Espace opticien')));
  }
}
